#include "security.h"

void security_init(void) {
    /* Phase 3 placeholder for credentials and session initialization. */
}

int security_login(const char *username, const char *password_hash) {
    (void)username;
    (void)password_hash;
    return 0;
}

